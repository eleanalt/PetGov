import React from "react";
import { Card, CardContent, Typography } from "@mui/material";

export default function LostPets() {
  return (
    <Card>
      <CardContent>
        <Typography variant="h5" gutterBottom>Lost Pets</Typography>
        <Typography variant="body2">Placeholder page (θα συνδεθεί με /lostPets).</Typography>
      </CardContent>
    </Card>
  );
}

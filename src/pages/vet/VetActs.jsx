import React from "react";
import { Card, CardContent, Typography } from "@mui/material";

export default function VetActs() {
  return (
    <Card>
      <CardContent>
        <Typography variant="h5" gutterBottom>Ιατρικές πράξεις</Typography>
        <Typography variant="body2">Placeholder page (θα συνδεθεί με /medicalActs).</Typography>
      </CardContent>
    </Card>
  );
}
